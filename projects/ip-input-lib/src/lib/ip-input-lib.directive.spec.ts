import { IpInputLibDirective } from './ip-input-lib.directive';

describe('IpInputLibDirective', () => {
  it('should create an instance', () => {
    const directive = new IpInputLibDirective();
    expect(directive).toBeTruthy();
  });
});
